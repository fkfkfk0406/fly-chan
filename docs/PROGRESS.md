# 진행 상황과 다음 할 일

다른 PC나 새 대화에서 이어서 작업할 때 이 문서부터 읽으세요. 프로젝트 설명과 실행 방법은 [README](../README.md)에 있습니다.

## 지금 상태 (2026-09-16)

- 최신 커밋: `0a5fad0`
- 테스트 51개 통과 (`npm test`), 타입 검사 통과 (`npx tsc --noEmit -p .`)
- 동작 확인은 헤드리스 브라우저(데스크톱 1280~1600px, 휴대폰 420px)로 했습니다.

### 완성된 기능
- **뇌:** FlyWire v783 전뇌(뉴런 138,639개)를 LIF로 Web Worker에서 실시간 시뮬. 논문 결과 재현(당→MN9, 쓴맛 억제, JO-F→그루밍, 루밍→Giant Fiber)을 테스트로 고정.
- **몸·방:** VRM 캐릭터(더듬이·날개), 아늑한 방 3D, 걷기·먹기·그루밍·도약·침대에서 자기, 낮/밤 조명.
- **돌봄:** 배고픔·졸림·기분·청결·애정 게이지, 닫아 둔 시간 반영(최대 72시간), 일기.
- **관계:** 낯섦→친구→호감→두근두근→연인 5단계, 단계 이벤트와 선택지 대화, 시간대·복귀 인사, 단계별 대화 주제와 혼잣말, 얼굴 클로즈업·표정·하트 파티클·효과음.
- **이름:** 첫 실행 때 캐릭터 이름과 호칭을 정하고, 대사에 받침 맞춘 조사로 들어감.
- **간식:** 딸기(당)·꿀(인두 당)·물(물 GRN)·짠 과자(Ir94e)·쓴 버섯(쓴맛)이 서로 다른 미각 뉴런을 자극. 먹는 동안 MN9 발화율을 기록해 일기에 "뇌가 알려 준 취향"으로 표시.
- **하트·상점:** 방치·돌봄으로 하트 적립, 간식 재고 구매, 꾸미기 4종(리본·목도리·화분·액자), 하루 한 번 선물.
- **업적:** 업적 10종과 누적 기록, 꾸미기의 하트 적립 보너스 (`src/care/Care.ts`). **아직 UI에 연결되지 않음.**

## 바로 이어서 할 일 (작은 것부터)

1. **업적·사진 UI 연결** (`0a5fad0`에서 이어서)
   - 📷 사진 찍기 버튼: `BodyScene`의 캔버스를 그 프레임에 바로 `toDataURL`로 저장해 PNG 다운로드 (`preserveDrawingBuffer` 없이 하려면 render 직후 같은 틱에서 호출).
   - 찍은 사진을 축소해 `care.s.photo`에 저장하고, 상점에서 산 🖼️ 액자 메시의 재질에 텍스처로 입히기.
   - 일기 패널에 업적 목록(달성/미달성, 보상) 표시.
   - `?hearts=999` 같은 디버그 옵션 (현재 `?dt` `?adapt` `?time`과 같은 방식).
   - 이벤트마다 `care.record(...)` 호출 연결: 식사·쓰담·청소·대화·수면·그루밍·사진.
2. **육성 데이터 내보내기/불러오기**: 저장 상태(JSON)를 파일로 내보내고 불러오는 버튼. PC를 옮겨도 이어서 키울 수 있게.
3. **데스크톱 앱 (Tauri)**: 아래 "구상" 참고. 1~2단계씩 끊어서 진행.
4. **스마트폰 배포 (PWA)**: 백그라운드에서 시뮬 정지, 데이터 압축·캐시, 호스팅(GitHub Pages는 private 저장소면 유료, Cloudflare Pages는 파일당 크기 제한) 결정 필요.
5. **버섯체 학습**: 도파민 뉴런과 Kenyon cell → MBON 가소성으로 "쓴 걸 줬던 기억"을 진짜 뇌에 남기기. 난이도 높음.

## 구조 빠르게 보기

| 파일 | 역할 |
|---|---|
| `src/sim/lif-engine.ts` | LIF 엔진 (적응 전류 옵션 포함). 순수 계산이라 Node에서도 돌아감 |
| `src/sim/sim.worker.ts` | 커넥톰 로드, 시뮬 루프, 프레임 전송 |
| `src/world/Habitat.ts` | 방·간식 정의(`SNACKS`)와 감각 입력 변환 |
| `src/behavior/Controller.ts` | DN 발화율 → 행동 (VNC 역할을 절차적으로 대신) |
| `src/care/Care.ts` | 돌봄 상태·관계 단계·하트·상점·업적 (뇌 밖의 게임 상태) |
| `src/story/` | 대본, 연출 감독, 이름 조사 처리 |
| `src/body/` | VRM 리그, 애니메이션, 방 3D |
| `pipeline/build_connectome.py` | 원본 데이터 → `public/data` 바이너리와 뉴런 그룹 |
| `scripts/` | probe(자극 실험), bench(속도), taste_probe·snack-safety(미각), adapt(적응) |

## 조사해서 알아낸 것 (다시 하지 마세요)

- **후각은 못 씁니다.** ORN을 10 Hz로만 자극해도 자극을 끊은 뒤 약 1만 개 뉴런이 계속 발화합니다. 원인은 Kenyon cell이 아니라 더듬이엽 국소 뉴런(ALLN)이고, 이 뉴런들은 신경전달물질 예측 신뢰도가 평균 0.46으로 낮습니다.
- **적응 전류로는 해결 안 됩니다.** 폭주를 멈출 만큼 세게 걸면 섭식 반응(MN9)이 같이 죽습니다. 그래서 기본값은 꺼져 있고 `?adapt=300:1`로만 켭니다.
- **두 가지 맛을 동시에 넣으면 폭주합니다**(특히 Ir94e + 쓴맛). 그래서 입에 닿은 간식 중 하나만 자극합니다.
- **세기 제한:** 물 GRN은 100 Hz에서 무반응이라 200 Hz, Ir94e는 200 Hz에서 폭주라 100 Hz.
- **논문 노트북의 당·쓴맛 ID는 옛 버전(v630)이라 지금 데이터(v783)에 없습니다.** 물·Ir94e ID만 v783에 그대로 있어 18개씩 대조해 씁니다.

## 데스크톱 앱 구상 (아직 구현 안 함)

- **Tauri 추천** (설치 5~10 MB, OS 웹뷰 사용). Electron은 150 MB 이상이라 상주 앱에는 부담.
- 360×640 작은 창, 프레임 없음, 항상 위, 드래그 이동, 트레이 상주, 자동 시작 옵션.
- 창을 숨기면 뇌 시뮬을 멈추고 돌아올 때 경과 시간만 반영(오프라인 계산은 이미 있음).
- 뇌 데이터 91 MB: 설치 파일에 포함할지 첫 실행 때 받을지 결정 필요.
- 저장은 `localStorage` → 사용자 폴더 JSON 파일로 바꾸는 것이 안전.
- 순서: 껍데기 → 창 설정 → 트레이·시뮬 정지 → 저장·데이터 → 알림.

## 작업 방식 메모

- 답변은 한국어로.
- 오래 걸리는 확인(타입 검사·테스트·데스크톱/휴대폰 브라우저)은 한꺼번에 병렬로 돌리기.
- 커밋·푸시는 요청받았을 때만.
- 큰 기능은 한 번에 몰아서 하지 말고 1~2단계씩 (확인 과정에서 토큰이 많이 듭니다).
